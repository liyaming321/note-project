# 个人知识库项目开发任务清单

## 项目概述
构建一个运行在个人电脑上的知识库系统，用于记录代码片段和 Markdown 笔记，支持全文检索、版本历史、标签分类等。系统采用 Java 后端（Spring Boot） + Vue 前端，数据存储于本地 H2 数据库，全文检索基于 Apache Lucene，最终打包为单一可执行 Jar，离线使用。

## 技术栈
- 后端：Java 17+ / Spring Boot 3.x / Spring Data JPA / H2 (Embedded) / Apache Lucene / IK Analyzer
- 前端：Vue 3 + Vite + Ant Design Vue + Vditor (Markdown 编辑器) / highlight.js
- 构建：Maven，前后端分离后合并为静态资源

---

## 第一阶段：基础项目搭建与笔记 CRUD （MVP）

- [x] **任务 1.1：初始化 Spring Boot 项目**
  - 使用 Spring Initializr 创建项目，依赖：Spring Web, Spring Data JPA, H2 Database, Lombok, Validation
  - 配置 application.yml（端口 8080、H2 文件存储路径 `~/.knowledge-base/data`、控制台关闭）
  - 项目结构分层：controller / service / repository / entity

- [x] **任务 1.2：创建核心实体与数据库表**
  - Note 实体（id, title, content, contentText, type, language, tags, categoryId, pinned, favorite, isDeleted, createdAt, updatedAt）
  - Category 实体（id, name, parentId）
  - Tag 实体（id, name，若使用独立标签表）
  - 使用 JPA 自动建表，并初始化若干分类

- [x] **任务 1.3：实现笔记基础 CRUD API**
  - `POST /api/notes` – 创建笔记，支持接收 Markdown/代码内容，自动提取纯文本至 contentText
  - `GET /api/notes/{id}` – 获取详情
  - `PUT /api/notes/{id}` – 更新笔记（暂不处理版本历史）
  - `DELETE /api/notes/{id}` – 逻辑删除（isDeleted = true）
  - `GET /api/notes` – 分页列表，支持按标签、分类、类型过滤，按创建/更新时间排序
  - 请求/响应 DTO 封装，统一返回格式 `ApiResponse<T>`

- [x] **任务 1.4：标签与分类管理 API**
  - `GET /api/categories` – 获取所有分类（树形结构或扁平）
  - `POST /api/categories` – 添加分类
  - `GET /api/tags` – 获取所有标签
  - `POST /api/tags` – 添加标签

- [x] **任务 1.5：搭建前端项目框架**
  - 使用 Vite 创建 Vue 3 项目，集成 Ant Design Vue、Vue Router、Axios
  - 完成基础布局：侧边栏导航（笔记列表、分类筛选、标签云）、主内容区
  - 配置代理转发 API 请求到后端 8080 端口

- [x] **任务 1.6：笔记列表与详情页面**
  - 笔记列表页：展示标题、标签、更新时间、摘要，支持分页和简单筛选
  - 笔记详情页：Markdown 渲染展示，代码块语法高亮（使用 highlight.js）
  - 实现删除、收藏/置顶切换按钮

- [x] **任务 1.7：笔记创建与编辑页面**
  - 使用 Vditor 作为 Markdown 编辑器，支持实时预览、代码块语言选择、全屏编辑
  - 提交时提取标题（从正文首行或手动输入）、标签、分类、语言等元数据
  - 支持编辑已有笔记

- [x] **任务 1.8：前后端联调与基本验证**
  - 完整流程测试：创建笔记 -> 列表展示 -> 点击查看 -> 编辑 -> 删除 -> 恢复
  - 确保 H2 数据在重启后持久存在

---

## 第二阶段：全文搜索集成

- [x] **任务 2.1：集成 Lucene 并配置中文分词**
  - 添加依赖 `lucene-core`、`lucene-analyzers-smartcn`（或引入 IK Analyzer）
  - 创建 `IndexService`，负责索引的创建、更新、删除
  - 定义索引字段：id(StringField)、title(TextField,加权)、contentPlain(TextField)、contentCode(TextField)、tags(StringField)、category(TextField)、language(StringField)、createdTime(LongPoint)

- [x] **任务 2.2：实现索引与数据库同步**
  - 在笔记创建、更新、删除时，异步或同步调用 IndexService 更新索引
  - 应用启动时检查索引目录是否存在，若不存在则全量重建索引
  - 提供管理接口 `POST /api/admin/reindex` 用于手动重建

- [x] **任务 2.3：开发搜索 API**
  - `GET /api/search?q=keyword&scope=all/title/code&tag=&category=&page=`
  - 使用 MultiFieldQueryParser，标题权重设为 2，支持按 scope 限制搜索字段
  - 结果返回高亮片段（highlight）、笔记基本信息、命中字段标识
  - 支持按标签、分类、语言过滤

- [x] **任务 2.4：前端搜索界面**
  - 顶部搜索栏，支持输入关键词，选择搜索范围（全部/标题/代码）
  - 搜索结果列表：展示标题、高亮片段、标签，点击跳转到详情
  - 详情页中点击标签可触发搜索该标签

---

## 第三阶段：版本历史

- [x] **任务 3.1：创建版本历史表与实体**
  - NoteHistory 实体 (id, noteId, title, content, version, createdAt)
  - 与 Note 建立多对一关系

- [x] **任务 3.2：修改更新逻辑以实现自动版本保存**
  - 更新笔记时，将当前版本完整保存至 NoteHistory 表，版本号递增
  - 可配置最大保留版本数（如 50），超出则删除最旧版本
  - 版本保存操作需在事务内完成

- [x] **任务 3.3：版本历史 API**
  - `GET /api/notes/{id}/history` – 返回版本列表（版本号、保存时间）
  - `GET /api/notes/{id}/history/{version}` – 获取特定版本内容
  - `POST /api/notes/{id}/revert/{version}` – 将笔记恢复到指定版本（创建新版本保存当前内容）

- [x] **任务 3.4：前端版本历史界面**
  - 笔记详情页增加“历史版本”侧边栏或按钮
  - 版本列表展示时间与版本号，点击可预览该版本内容（区分显示）
  - 提供“恢复此版本”按钮

---

## 第四阶段：导入导出与体验优化

- [x] **任务 4.1：Markdown 导入解析**
  - `POST /api/import/markdown` – 上传 .md 文件，解析 YAML front matter 作为元数据（title, tags, category, language），内容作为正文
  - 支持批量上传（多个文件或 zip）

- [x] **任务 4.2：Markdown 导出**
  - `GET /api/notes/{id}/export/markdown` – 将笔记导出为格式良好的 .md 文件下载
  - 批量导出 `POST /api/export/zip` – 接收 id 列表，打包多个 .md 成 zip 下载

- [x] **任务 4.3：收藏与置顶功能完善**
  - 前端在列表和详情页添加星标（收藏）和图钉（置顶）图标，调用对应更新接口
  - 列表增加排序选项：按更新时间、创建时间、标题，支持仅显示收藏/置顶

- [x] **任务 4.4：UI/UX 优化**
  - 笔记列表支持拖拽排序（自定义排序）
  - 代码块一键复制按钮
  - 编辑器图片粘贴上传支持（存储至本地 `~/.knowledge-base/images`，返回相对路径）
  - 快捷键支持（如 Ctrl+S 保存笔记）

- [x] **任务 4.5：打包与部署配置**
  - Maven 配置将前端构建产物拷贝到 Spring Boot 的 static 目录
  - 打包为单一 fat jar，启动后自动启动 H2 和 Lucene
  - 编写启动脚本（Windows .bat / Linux .sh），指定数据目录变量
  - 测试 jar 独立运行，无任何外部依赖

---

## 第五阶段：可选增强（视需要执行）

- [ ] **任务 5.1：代码片段优化**
  - 自动识别代码语言（通过 highlight.js 语言别名检测）
  - 代码搜索页：独立展示所有代码块，支持按语言筛选、复制

- [ ] **任务 5.2：命令行工具（CLI）**
  - 使用 Picocli 开发简单 CLI，通过调用本地 REST API 实现快速记录、搜索
  - 支持 `kb add "title" -t tag1,tag2 -c category < content.md` 类似命令

- [x] **任务 5.3：数据备份与恢复**
  - 一键备份整个数据目录（数据库 + 索引 + 图片）打包为 zip
  - 提供恢复接口或启动参数指定备份文件路径

- [ ] **任务 5.4：相似笔记推荐**
  - 基于 Lucene 的 MoreLikeThis 查询，在笔记详情页展示相关笔记
  - 基于标签重合度简单推荐

- [x] **任务 5.5：浏览器书签导入（可选）**
  - 解析浏览器导出的 HTML 书签文件，转为笔记存储

---

## 第六阶段：原型功能补齐与工作台增强

- [x] **任务 6.1：草稿与发布状态**
  - Note 增加 status 状态，支持草稿、已发布两种列表展示状态
  - 已发布笔记使用原型中的白色标准卡片，草稿笔记使用蓝色强调卡片
  - 新增发布、撤回为草稿、仅看草稿、仅看已发布等前后端操作与筛选

- [x] **任务 6.2：归档功能**
  - Note 增加 archived 状态，支持归档与取消归档
  - 新增归档筛选与归档列表，归档笔记默认不出现在“所有笔记”
  - 前端侧栏“归档”入口展示归档笔记，并支持恢复到普通笔记

- [x] **任务 6.3：回收站独立视图**
  - 列表接口支持仅查看已删除笔记
  - 回收站页面支持恢复、批量恢复、永久删除
  - 删除、恢复、永久删除行为需要与 Lucene 索引同步

- [x] **任务 6.4：集合/分类管理完善**
  - 前端提供“新建分类”入口，支持创建分类
  - 支持分类重命名、删除前占用校验、父子分类管理
  - 分类变更后列表、编辑页、导入流程同步刷新

- [x] **任务 6.5：更新时间范围筛选**
  - 列表与搜索接口支持按今天、近 7 天、近 30 天、自定义时间范围筛选
  - 前端筛选面板增加更新时间筛选，并与 URL 查询参数同步
  - MockMvc 覆盖时间范围边界与分页组合查询

- [x] **任务 6.6：设置与维护页面**
  - 新增设置页面，展示数据目录、索引目录、图片目录和版本信息
  - 提供手动重建索引、备份下载、备份恢复说明入口
  - 前端侧栏“设置”与顶部设置图标跳转到该页面

- [x] **任务 6.7：帮助与快捷键面板**
  - 新增帮助弹窗，展示常用操作、快捷键、导入导出说明
  - 顶部帮助图标打开帮助面板
  - 补充前端交互验证，确保桌面端和移动端可用

---

## 第七阶段：智能搜索与知识发现

- [ ] **任务 7.1：统一搜索入口与搜索模式升级**
  - 梳理当前 Lucene 全文检索、数据库结构化筛选、标签/分类筛选的职责边界
  - 前端搜索区增加搜索模式切换：精确全文搜索、语义搜索、混合搜索
  - 搜索结果展示命中来源、命中字段、相关度说明和当前筛选条件摘要
  - 保持 URL 查询参数同步，支持刷新后恢复搜索状态

- [x] **任务 7.2：Embedding 向量化基础能力**
  - 新增 EmbeddingProvider 抽象，默认使用 `local-cli` 调用本地 llama.cpp `llama-embedding` 命令行工具
  - 支持用户手动下载 GGUF Embedding 模型，并通过环境变量配置可执行文件路径、模型路径、池化、归一化、超时和批大小
  - 新增笔记向量生成流程：创建、更新、恢复、导入、归档、删除、永久删除、重建索引时同步刷新或移除向量
  - 向量索引默认独立存储到 `~/.knowledge-base/vector-index`，使用 Lucene `KnnFloatVectorField` 保存向量，配置缺失时自动降级为仅 Lucene 全文搜索
  - 设置页面展示向量索引目录、供应商、模型名称、向量维度、池化、归一化、最近重建时间和配置状态

- [x] **任务 7.3：语义搜索 API**
  - 新增语义搜索接口，支持按自然语言问题匹配语义相近笔记
  - 支持分类、标签、语言、发布状态、更新时间范围等现有筛选条件
  - 搜索结果返回语义相似度、匹配原因摘要、笔记基本信息和可跳转来源
  - MockMvc 覆盖未配置 Embedding、空索引、正常命中、筛选组合和分页边界

- [ ] **任务 7.4：混合搜索排序**
  - 将 Lucene BM25 关键词得分与向量相似度得分合并，形成混合搜索排序
  - 支持标题命中、标签命中、置顶、收藏、更新时间等轻量加权规则
  - 后端返回排序解释字段，便于前端展示“为什么命中这篇笔记”
  - 增加重建索引后的搜索一致性验证，确保删除、归档、恢复不会出现脏结果

- [ ] **任务 7.5：基于笔记的智能问答（RAG）**
  - 新增知识库问答接口：根据问题检索相关笔记片段，再调用阿里百炼或 DeepSeek 生成回答
  - 回答必须返回引用来源，包括笔记 ID、标题、命中片段和详情页链接
  - 前端新增“问知识库”面板，支持连续提问、查看引用、跳转原文
  - 未配置 LLM 时显示清晰的配置引导，不影响普通搜索和笔记使用

- [ ] **任务 7.6：相似笔记与知识发现**
  - 详情页展示相似笔记，优先使用向量相似度，未配置向量时回退到标签重合度和 Lucene MoreLikeThis
  - 新增“可能相关”区域，展示同分类、同标签、语义相近、最近更新的笔记
  - 支持从相似笔记跳转，并保持左侧工作台导航不变
  - 为相似笔记推荐补充后端服务测试和前端交互验证

- [ ] **任务 7.7：搜索体验与运维完善**
  - 搜索页面增加搜索历史、常用搜索、清空历史和一键复制搜索条件
  - 设置页提供重建 Lucene 索引、重建向量索引、清理无效向量、检查索引健康状态
  - README 补充智能搜索、语义搜索、LLM 问答的配置方式和隐私说明
  - `docs/implementation-log.md` 按任务记录实现过程、验证命令、结果和遗留事项

- [ ] **任务 7.8：导入数据完善增加链接导入**
  - 增加导入链接功能，自动去网页查看 把网页内容进行整理，调用大模型 自动生成标题/摘要/标签等
  - 如果痛过链接过去到数据，则进入到新建笔记页 预览本次执行的结果
  - README 补充相关说明说明
  - `docs/implementation-log.md` 按任务记录实现过程、验证命令、结果和遗留事项
