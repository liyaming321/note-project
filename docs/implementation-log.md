# 实现记录

## 2026-05-13 任务 1.1：初始化 Spring Boot 项目

- 新增 Maven 后端工程，基础包名为 `com.knowledgebase`。
- 配置 Spring Boot 3.5.11、Java 17、Spring Web、Spring Data JPA、Validation、H2、Lombok、Test。
- 新增 `application.yml`，端口为 `8080`，H2 文件库路径为 `~/.knowledge-base/data/knowledge-base`，H2 Console 关闭。
- 验证：`mvn test` 通过。

## 2026-05-13 任务 1.2：创建核心实体与数据库表

- 新增 `Note`、`Category`、`Tag`、`NoteType` 实体，标签采用独立表，笔记与标签为多对多关系。
- 实体和字段补充 Hibernate 注释，JPA 使用 `ddl-auto=update` 自动建表。
- 新增启动初始化器，默认创建“默认分类”“代码片段”“学习笔记”。
- 验证：`mvn test` 通过，测试环境使用 H2 内存库隔离本机文件库。

## 2026-05-13 任务 1.3：实现笔记基础 CRUD API

- 新增统一响应 `ApiResponse<T>` 和分页响应 `PageResponse<T>`。
- 新增笔记创建、详情、更新、逻辑删除、恢复、分页查询接口。
- 分页查询支持标签、分类、类型、收藏、置顶、是否包含已删除、创建/更新时间/标题排序。
- Markdown 内容保存时自动提取纯文本 `contentText`。
- 验证：MockMvc 覆盖创建、查询、更新、删除、恢复、列表过滤。

## 2026-05-13 任务 1.4：标签与分类管理 API

- 新增分类树查询和分类创建接口。
- 新增标签查询和标签创建接口。
- 重复分类、重复标签返回业务错误；父分类不存在返回 404。
- 验证：MockMvc 覆盖分类和标签创建，以及笔记关联分类/标签流程。

## 2026-05-13 任务 1.5：搭建前端项目框架

- 新增 `frontend/` Vue 3 + Vite + TypeScript 工程文件。
- 集成 Ant Design Vue、Vue Router、Axios、Vditor、highlight.js。
- Vite 代理 `/api` 到 `http://localhost:8080`。
- 注意：本机当前 Node 为 20.18.0，低于 Vue 最新脚手架要求，因此前端依赖固定在兼容 Vite 5 的版本；本次 `npm install` 在当前环境长时间无输出卡住，尚未生成 `node_modules`。

## 2026-05-13 任务 1.6：笔记列表与详情页面

- 新增笔记列表页，支持分类、标签、类型、收藏、置顶、已删除过滤与分页。
- 新增笔记详情页，支持 Markdown 渲染、代码高亮、删除、恢复、收藏、置顶、编辑跳转。
- 验证：源码完成；前端构建受 `npm install` 卡住影响，尚未完成。

## 2026-05-13 任务 1.7：笔记创建与编辑页面

- 新增笔记创建/编辑页，使用 Vditor 编辑 Markdown 内容。
- 支持标题、类型、语言、分类、标签、收藏、置顶元数据。
- 验证：源码完成；前端构建受 `npm install` 卡住影响，尚未完成。

## 2026-05-13 任务 1.8：前后端联调与基本验证

- 后端自动化验证已覆盖创建笔记、列表展示、详情查看、编辑、删除、恢复。
- 待完成：前端依赖安装成功后运行 `npm run build`，并启动前后端进行浏览器端完整手工验证。

## 2026-05-13 联调修复记录

- 修复 H2 文件库连接串，移除 `AUTO_SERVER=TRUE`，避免 H2 2.3.x 启动失败。
- 重新调整 H2 文件库连接串为 `AUTO_SERVER=TRUE`，用于解决多个本地进程访问同一数据库文件时的锁冲突。
- 修复笔记标签复用逻辑，保证新标签会真正落库后再返回。
- 为 `Category`、`Tag`、`Note` 显式补充 getter，避免本地 Lombok 注解处理未生效时出现编译失败。
- 为 `DataInitializer`、`service`、`controller` 显式补充构造器，避免本地 Lombok 构造器生成未生效时出现 `final` 字段未初始化错误。
- 为 `Category`、`Tag`、`Note` 显式补充 JPA 无参构造器，避免 Hibernate 运行期实例化实体失败。
- 前端依赖已成功安装并通过 `npm run build`。
- 前端开发服务器已在 `http://localhost:5173/` 启动，后端已在 `http://localhost:8080/` 启动。

## 2026-05-13 前端蓝白主题改版

- 重做首页为蓝白知识工作台布局，加入顶部 Hero、侧边筛选面板、统计条和更清晰的笔记卡片。
- 重做详情页与编辑页，统一为蓝白玻璃卡片风格，强化操作区、摘要区和正文区的层次。
- 全局样式改为蓝白主题变量体系，统一按钮、标签、边框、阴影与响应式布局。

## 2026-05-13 任务 2.1：集成 Lucene 并配置中文分词

- 新增 Lucene 9.12.2 依赖：`lucene-core`、`lucene-analysis-common`、`lucene-analysis-smartcn`、`lucene-queryparser`、`lucene-highlighter`。
- 新增 `SearchIndexConfig`，使用 `SmartChineseAnalyzer` 作为中文分词分析器。
- 新增 `knowledge-base.index-path` 配置，默认索引目录为 `~/.knowledge-base/index`，测试环境使用临时索引目录隔离。
- 新增 `SearchIndexFields` 常量，统一维护 id、title、contentPlain、contentCode、tags、category、language、createdTime 等索引字段。
- 验证：`mvn test` 通过。

## 2026-05-13 任务 2.2：实现索引与数据库同步

- 新增 `IndexService`，支持笔记索引创建、更新、删除和全量重建。
- 笔记创建、更新、逻辑删除、恢复时同步维护 Lucene 索引；逻辑删除会移除对应索引，恢复会重新写入索引。
- 新增 `SearchIndexInitializer`，应用启动时检查索引是否存在，缺失时自动全量重建。
- 新增管理接口 `POST /api/admin/reindex`，用于手动重建索引并返回索引数量与索引目录。
- 验证：MockMvc 覆盖删除后搜索不可见、恢复后可搜索、手动重建接口可用。

## 2026-05-13 任务 2.3：开发搜索 API

- 新增 `GET /api/search`，支持 `q`、`scope=all/title/code`、`tag`、`category`、`language`、`page`、`size` 参数。
- 搜索使用 `MultiFieldQueryParser`，标题字段查询权重为 2；搜索结果返回高亮片段、命中字段和笔记基础信息。
- 高亮片段使用 HTML 编码器，仅保留 `<mark>` 高亮标签，避免搜索结果展示原始 HTML。
- 支持标签、分类 ID/分类名、语言精确筛选；标签点击场景支持不输入关键词，仅按标签筛选。
- 验证：MockMvc 覆盖标题搜索、代码搜索、标签/分类/语言组合筛选、逻辑删除与恢复闭环。

## 2026-05-13 任务 2.4：前端搜索界面

- 首页新增顶部全文搜索栏，支持关键词输入、搜索范围切换，以及标签、分类、语言筛选。
- 笔记列表页新增搜索结果模式，展示高亮片段、命中字段、标签，并可点击跳转详情。
- 列表卡片和详情页标签点击会进入搜索模式，按标签筛选相关笔记。
- 保留原有列表浏览模式和侧栏分类/标签筛选，搜索模式下隐藏列表专属筛选，避免无效操作。
- 验证：`cd frontend && npm run build` 通过；存在 Vite 大包体积提示，来自 Vditor/Ant Design 体积，不影响构建结果。

## 2026-05-13 任务 3.1：创建版本历史表与实体

- 新增 `NoteHistory` 实体和 `NoteHistoryRepository`，使用 `note_id + version` 作为唯一约束。
- 历史快照保存标题、正文、纯文本、类型、语言、分类快照和标签 JSON 快照，满足版本完整恢复需要。
- 新增历史版本列表与详情 DTO，便于前端按版本展示和预览。
- 验证：`mvn test` 通过。

## 2026-05-13 任务 3.2：修改更新逻辑以实现自动版本保存

- 在笔记 `PUT` 更新前自动保存当前版本为历史记录，版本号按笔记维度递增。
- 新增历史最大保留数量配置 `knowledge-base.history-max-versions`，默认 50，测试环境设为 3。
- 超出保留数量时自动裁剪最旧历史版本，保持历史表可控增长。
- 验证：MockMvc 覆盖多次更新后的历史裁剪逻辑，`mvn test` 通过。

## 2026-05-13 任务 3.3：版本历史 API

- 新增 `GET /api/notes/{id}/history`、`GET /api/notes/{id}/history/{version}`、`POST /api/notes/{id}/revert/{version}`。
- 恢复指定版本时，先保存当前版本，再回写标题、内容、纯文本、类型、语言、分类和标签，并同步 Lucene 索引。
- 历史版本不存在时返回 404，和现有资源未找到异常保持一致。
- 验证：MockMvc 覆盖版本查询、版本恢复、恢复后搜索结果仍可命中。

## 2026-05-13 任务 3.4：前端版本历史界面

- 详情页新增历史版本侧边栏，展示版本号、标题和保存时间。
- 支持点击历史版本预览内容，高亮显示当前预览版本，并提供“恢复此版本”操作。
- 预览历史版本时标签可继续跳转到搜索页，保留知识库浏览链路。
- 验证：`cd frontend && npm run build` 通过；当前仅保留 Vite 的大包体积提示。

## 2026-05-13 任务 4.1：Markdown 导入解析

- 新增 `POST /api/import/markdown`，支持单个 `.md` 文件和 `.zip` 压缩包批量导入。
- 解析 YAML front matter 的 `title`、`tags`、`category`、`language` 字段，正文作为笔记内容。
- 导入时自动创建缺失分类，并复用现有笔记创建流程同步写入检索索引。
- 验证：`mvn test` 通过，覆盖单文件导入和 ZIP 批量导入。

## 2026-05-13 任务 4.2：Markdown 导出

- 新增 `GET /api/notes/{id}/export/markdown` 导出单篇笔记为 Markdown 文件。
- 新增 `POST /api/export/zip` 批量导出指定笔记为 ZIP 包。
- 导出内容包含 front matter，便于后续再次导入。
- 验证：`mvn test` 通过，覆盖单篇导出和 ZIP 导出。

## 2026-05-13 任务 4.3：收藏与置顶功能完善

- 列表页和详情页的收藏、置顶改为可点击图标按钮，直接调用现有接口。
- 列表新增 `sortOrder` 自定义排序，支持拖拽重排并持久化。
- 列表排序选项补充自定义排序，并保留更新时间、创建时间和标题排序。
- 验证：`mvn test` 通过，覆盖状态切换和排序接口。

## 2026-05-13 任务 4.4：UI/UX 优化

- 列表页支持拖拽排序，拖拽后会写回自定义排序值。
- 详情页增加代码块复制按钮，并在 Markdown 预览后自动挂载复制控件。
- 编辑器支持图片粘贴上传，文件保存到本地 `~/.knowledge-base/images`。
- 编辑页加入 `Ctrl/Cmd + S` 快捷保存，并接入 Vditor 图片上传配置。
- 验证：`cd frontend && npm run build` 通过。

## 2026-05-13 任务 4.5：打包与部署配置

- Maven 增加前端构建、静态资源拷贝和单 Jar 打包流程。
- 新增 `scripts/start.sh` 与 `scripts/start.bat`，支持通过环境变量指定数据、索引和图片目录。
- 增加 SPA 路由转发，打包后的 Jar 可直接访问 Vue 页面和后端 API。
- 验证：`mvn package` 通过；Jar 独立启动后，`/`、`/notes/1` 和 `/api/categories` 均可正常访问。

## 2026-05-13 任务 5.3：数据备份与恢复

- 新增 `GET /api/admin/backup`，可下载包含 `data/`、`index/`、`images/` 的完整备份 ZIP。
- 备份时跳过 H2 与 Lucene 的运行时锁文件，避免打包不可恢复的临时锁。
- 新增启动参数 `knowledge-base.restore-backup-path`，可在应用启动早期恢复指定备份 ZIP。
- 将 H2 数据路径统一纳入 `knowledge-base.data-path` 配置，便于脚本和备份恢复逻辑共用。
- 前端首页新增“备份数据”按钮，可直接下载备份文件。
- 验证：`mvn test` 通过，覆盖备份 ZIP 包含数据、索引、图片目录；`cd frontend && npm run build` 通过。

## 2026-05-13 任务 5.5：浏览器书签导入

- 新增 `POST /api/import/bookmarks`，支持上传浏览器导出的 Netscape Bookmark HTML。
- 解析 `<H3>` 文件夹作为分类，解析 `<A HREF>` 为 Markdown 笔记，自动打上 `bookmark` 标签。
- 每条书签笔记正文包含标题、原始链接、来源和原始创建时间戳。
- 前端首页新增“导入书签”按钮，支持选择 `.html` / `.htm` 文件。
- 验证：`mvn test` 通过，覆盖书签 HTML 导入为笔记并可按 `bookmark` 标签检索；`cd frontend && npm run build` 通过。

## 2026-05-13 原型工作台前端改版与第六阶段规划

- 阅读 `/Users/lym/Downloads/stitch_personal_knowledge_base_redesign` 下的 `DESIGN.md`、`code.html` 和 `screen.png`，以 Cognitive Flow 原型作为当前前端改版依据。
- 首页改为固定侧栏 + 顶部搜索操作栏 + 主内容画布的工作台布局，保留新建、导入 Markdown、导入书签、备份、导出、搜索、筛选、收藏、置顶和拖拽排序能力。
- 新增网格 / 列表视图切换和“项目焦点”卡片，优先展示置顶笔记，其次收藏笔记，再其次当前页第一条笔记。
- 全局样式改为原型蓝白 token，统一列表、详情、编辑、历史版本、Markdown 阅读区和移动端底部导航视觉。
- 对比原型后，将归档、独立回收站、集合/分类管理、更新时间范围筛选、设置维护页、帮助快捷键面板追加为 `task.md` 第六阶段任务。
- 验证：`cd frontend && npm run build` 通过；仍存在 Vite 大包体积提示，来自 Vditor/Ant Design 等大依赖，不影响构建结果。

## 2026-05-13 发布态卡片修正与草稿状态规划

- 根据原型截图反馈，红圈中的白色标准卡片属于已完成 / 已发布状态，蓝色强调卡片应仅用于草稿状态。
- 当前 Note 暂无草稿 / 发布状态字段，因此移除列表页自动展示蓝色草稿卡的逻辑，现有列表统一按白色发布态卡片展示。
- 将“草稿与发布状态”追加为 `task.md` 第六阶段任务，后续补充 status 字段、发布 / 撤回操作、状态筛选和对应卡片展示。

## 2026-05-13 新版 UI 功能按钮中文化

- 将工作台顶部操作按钮从 `Create`、`Import`、`Bookmark`、`Backup`、`Export` 调整为中文：新建笔记、导入、导入书签、备份、导出。
- 将详情页和编辑页标题辅助文案从英文调整为中文，头像占位从 `KB` 调整为“知”。
- 保留 `Markdown`、`java / js / sql` 等技术格式名和示例，不作为普通按钮文案翻译。

## 2026-05-13 第六阶段原型功能补齐

- 后端新增笔记 `status` 发布状态和 `archived` 归档状态，提供发布 / 转草稿、归档 / 取消归档、回收站仅删除查询、批量恢复、永久删除接口。
- Lucene 索引新增发布状态和更新时间字段；归档或删除笔记会从索引移除，重建索引仅处理未删除且未归档笔记。
- 列表与搜索接口支持 `status`、`updatedFrom`、`updatedTo` 筛选；分类接口支持重命名和删除，并校验子分类与笔记占用；管理接口新增 `/api/admin/workspace`。
- 前端工作台接入草稿蓝色卡片、发布白色卡片、归档入口、回收站入口、当前页批量恢复、永久删除、更新时间筛选、集合管理弹窗、帮助面板和独立设置维护页面。
- 编辑页支持选择“已发布 / 草稿”，详情页支持发布状态切换、归档切换、恢复和永久删除。
- SPA 路由转发补充 `/settings`，保证 Jar 或 Spring Boot 直接运行时能打开设置维护页。
- 设置页改为左侧功能列表 + 右侧功能区的访问方式，保留工作区概览、索引维护、备份恢复和帮助说明四个入口。
- `task.md` 第六阶段 6.1 到 6.7 已勾选完成；`docs/sql-changes.sql` 追加 `notes.status` 与 `notes.archived` 字段记录。
- 启动真实本地 H2 文件库时发现旧数据无法直接新增非空列，已调整为 Hibernate 建表检查前补可空默认列 + 启动回填迁移，避免已有笔记阻塞升级。
- 验证：`cd frontend && npm run build` 通过；`mvn test` 通过，MockMvc 覆盖草稿 / 发布、归档、回收站、更新时间范围、分类管理和工作区维护接口。
- 遗留事项：Vite 仍提示前端产物 chunk 超过 500 kB，来源主要是 Vditor 与 Ant Design Vue 依赖体积，当前不影响运行。

## 2026-05-14 首页筛选区布局统一

- 将首页筛选区统一为三行四列等宽网格，补齐分类、标签、语言、发布状态的字段标题。
- 更新时间自定义开始 / 结束日期控件改为固定占位，非自定义模式下禁用，避免筛选项切换时布局跳动。
- 快速筛选与重置按钮收纳到同宽功能单元格，并统一筛选卡内选择框、日期框和操作单元格高度。
- 预设更新时间筛选会清空自定义日期显示，避免禁用日期框残留旧值造成误解。
- 验证：`cd frontend && npm run build` 通过；使用系统 Chrome headless 截图检查首页筛选区等宽布局，截图保存到 `output/playwright/filter-layout.png`；本次仅调整前端筛选布局和文档，未执行后端测试。

## 2026-05-14 笔记卡片底部操作区对齐

- 统一笔记卡片底部日期、分类、归档状态、发布操作和归档操作的最小高度与行高。
- 将卡片底部区域改为垂直居中对齐，避免普通文本与 Ant Design 链接按钮高度不一致。
- 验证：`cd frontend && npm run build` 通过；使用系统 Chrome headless 截图检查卡片底部对齐，截图保存到 `output/playwright/card-footer-layout.png`；本次仅调整前端样式和文档，未执行后端测试。

## 2026-05-14 首页搜索与筛选区压缩

- 顶部搜索从连体搜索框改为输入框 + 独立搜索按钮，增加两者间距，避免按钮贴住输入框。
- 首页筛选卡默认仅展示分类、标签、语言三个常用筛选项。
- 类型、搜索范围、排序方式、方向、发布状态、更新时间、自定义日期、置顶、收藏、已删除筛选移动到“展开更多筛选”区域。
- 验证：`cd frontend && npm run build` 通过；使用系统 Chrome headless 截图检查默认折叠态，截图保存到 `output/playwright/filter-collapsed-compact.png`；本次仅调整前端样式和文档，未执行后端测试。

## 2026-05-14 README 项目说明文档

- 新增根目录 `README.md`，补充项目简介、功能概览、技术架构、本地开发、部署方式、常用命令、备份恢复和项目文档说明。
- 将工作台首页、笔记卡片和完整筛选面板截图复制到 `docs/images/`，并在 README 页面预览章节中引用。
- 验证：检查 README 中引用的截图文件均存在；本次仅新增文档和图片资源，未执行构建或后端测试。

## 2026-05-14 Git 忽略规则

- 新增根目录 `.gitignore`，忽略 macOS 系统文件、IDE 配置、Maven 构建产物、前端依赖与构建产物、本地运行数据、日志和临时测试截图目录。
- 保留 `.env.example` 与 `frontend/.env.example` 可提交，便于后续补充环境变量示例。
- 验证：检查当前常见生成目录 `.idea/`、`.atomcode/`、`target/`、`frontend/node_modules/`、`frontend/dist/`、`output/` 均已纳入忽略规则；本次仅新增配置和文档，未执行构建或后端测试。

## 2026-05-14 README 新建笔记截图补充

- 使用本地运行中的 8080 服务截取 `/notes/new` 新建笔记页面，保存为 `docs/images/note-editor.png`。
- 在 README 页面预览章节新增“新建笔记”说明，介绍标题、类型、语言、发布状态、分类、标签、收藏、置顶和 Vditor 正文编辑区。
- 验证：检查 README 图片链接 `docs/images/note-editor.png` 存在；本次仅更新文档和截图资源，未执行构建或后端测试。

## 2026-05-14 LLM 笔记总结

- 后端新增 `summary` 摘要字段，列表和详情响应均返回摘要；列表优先展示保存摘要，未维护摘要时继续使用正文截断摘要。
- 新增 LLM 配置，支持阿里百炼和 DeepSeek 的 OpenAI 兼容 Chat Completions 接口，环境变量可配置供应商、API Key、模型、Base URL 和超时时间。
- 新增 `GET /api/notes/llm-providers` 与 `POST /api/notes/llm-summary`，用于查询供应商配置状态和生成标题、摘要、标签、分类建议。
- 前端编辑页新增摘要输入框和 LLM 总结面板，支持选择阿里百炼或 DeepSeek，一键回填摘要、标签和已匹配分类。
- 详情页侧栏展示保存后的笔记摘要；README 补充 LLM 环境变量配置说明；`docs/sql-changes.sql` 追加 `notes.summary` 字段记录。
- 验证：`mvn test` 通过；`cd frontend && npm run build` 通过。Vite 仍提示大 chunk，来源为 Vditor / Ant Design Vue 依赖体积，不影响构建结果。

## 2026-05-14 分类入口文案统一

- 将工作台侧栏的“新建集合”统一改为“新建分类”，并同步调整分类管理弹窗标题、输入提示、按钮文案和操作提示。
- 保持分类创建与重命名逻辑不变，仅统一前端展示文案，避免页面中“集合 / 分类”混用。
- 验证：本次为纯文案调整，后续通过 `cd frontend && npm run build` 复核前端构建即可。

## 2026-05-14 侧栏品牌区与标签入口优化

- 工作台侧栏顶部增加上方留白，品牌副标题从“个人保险库”调整为“个人笔记知识库”。
- 移除左侧导航中的独立“标签”菜单，只保留下方标签云作为标签筛选入口，避免功能重复。
- 同步调整标签提示文案，移动端底部标签入口仍提示用户使用侧边栏标签云筛选。

## 2026-05-14 People Wiki 项目命名修正

- 将项目名称的旧拼写统一修正为 `People Wiki`。
- 将 Maven artifact、Spring 应用名、前端包名、启动脚本 Jar 路径和 README 部署命令统一修正为 `people-wiki`。
- 将测试临时目录和备份恢复临时目录前缀同步修正为 `people-wiki`，避免运行产物继续沿用旧拼写。
- 验证：`mvn clean package` 通过，生成 `target/people-wiki-0.0.1-SNAPSHOT.jar`。

## 2026-05-14 新建笔记工作台内嵌布局

- 将新建 / 编辑笔记页改为工作台布局，左侧固定功能导航保持不动，右侧展示笔记表单、LLM 总结和 Vditor 正文编辑区。
- 编辑页侧栏复用当前分类与标签数据，支持从侧栏跳回所有笔记、收藏、归档、回收站，以及按分类或标签进入工作台筛选。
- 列表页支持识别 `view` 查询参数，便于从编辑页侧栏回到对应导航视图。
- 验证：`cd frontend && npm run build` 通过；使用 Playwright 打开 `/notes/new` 确认左侧导航与右侧新建笔记同屏展示，截图保存为 `output/playwright/note-new-workspace.png`。

## 2026-05-14 新建笔记表单视觉收敛

- 去掉新建 / 编辑页右侧重复的大标题区域，保留顶部工作台标题和单一“基础信息”卡片，降低页面层级噪声。
- 调整基础信息栅格，标题、类型、语言、发布状态保持同一行对齐，分类、标签、收藏、置顶形成第二组信息区。
- 将 `LLM 总结` 从大面积蓝色提示块改为轻量 `AI 辅助` 工具条，使用低噪声背景、细边框和紧凑操作控件，使其更符合蓝白工作台主题。
- 验证：`cd frontend && npm run build` 通过；使用 Playwright 打开 `/notes/new` 检查新建页布局，截图保存为 `output/playwright/note-new-refined.png`。

## 2026-05-14 侧栏分类选中态统一

- 将左侧分类列表选中态从白色底改为浅蓝底、主蓝文字和左侧蓝色强调线，与主导航选中态保持同一蓝白主题。
- 分类 hover 态同步改为低透明浅蓝，避免与标签灰色胶囊和普通文本状态混淆。

## 2026-05-14 确认弹窗中文化

- 将笔记详情页删除、永久删除、历史版本恢复确认框的按钮文案统一为“确认 / 取消”。
- 将列表页永久删除笔记、分类管理删除分类确认框的按钮文案统一为“确认 / 取消”。
- 验证：`cd frontend && npm run build` 通过；`mvn process-resources` 通过，并已同步最新前端产物到 `target/classes/static`。

## 2026-05-14 笔记详情页工作台内嵌布局

- 将笔记详情页改为与新建 / 编辑页一致的工作台结构，左侧固定功能导航保持可见，右侧展示笔记标题、操作区、摘要信息、历史版本和 Markdown 正文。
- 详情页侧栏接入分类与标签数据，支持从当前详情页按分类、标签、收藏、归档、回收站和设置入口回到对应工作台视图。
- 保留详情页已有操作能力，包括编辑、导出、置顶、收藏、发布状态切换、归档、删除、恢复、永久删除、历史版本预览与恢复。
- 验证：`cd frontend && npm run build` 通过；`mvn process-resources` 通过；使用 Playwright 打开 `http://localhost:8080/notes/2`，确认左侧导航和右侧详情同屏展示。控制台仅有既有 `favicon.ico` 404。

## 2026-05-14 第七阶段任务规划

- 在 `task.md` 追加“第七阶段：智能搜索与知识发现”，规划统一搜索入口、Embedding 向量化、语义搜索 API、混合搜索排序、RAG 知识库问答、相似笔记推荐和搜索运维完善。
- 第七阶段延续当前 Lucene 全文检索能力，并规划可配置的向量索引和 LLM 问答能力，配置缺失时要求自动降级，不影响现有离线笔记功能。
- 验证：本次仅更新任务规划文档和实施日志，未修改功能代码，未执行构建或测试。

## 2026-05-14 7.2 本地命令行 Embedding 与向量索引

- 新增 `local-cli` Embedding Provider，支持通过环境变量配置 llama.cpp `llama-embedding` 可执行文件、GGUF 模型路径、池化方式、归一化和超时时间。
- 新增 Lucene 向量索引服务，使用独立目录 `~/.knowledge-base/vector-index` 和 `KnnFloatVectorField` 保存笔记向量，Lucene commit metadata 记录 provider、model、dimension、pooling、normalize 和最近重建时间。
- 将向量索引接入笔记创建、更新、恢复、导入、归档、删除、永久删除和批量恢复流程；未配置本地模型时自动跳过，不影响现有 CRUD 与 Lucene 全文搜索。
- 重建向量索引接入 `batch-size` 配置，批量调用 `llama-embedding --embd-separator --embd-output-format array`，并发读取标准输出和错误输出，降低大批量重建时反复加载模型与子进程输出阻塞的风险。
- 管理接口新增 Embedding Provider 状态、向量索引状态和手动重建向量索引；设置页新增“向量索引”维护面板，展示配置状态、模型、维度、索引数量和重建入口。
- 备份恢复增加 `vector-index/` 目录，README 补充本地 Embedding 环境变量配置，`task.md` 标记任务 7.2 完成。
- 新增 `VectorIndexServiceTest`，用模拟 `llama-embedding` 脚本验证本地命令行 Embedding 可批量重建 Lucene 向量索引。
- 验证：`cd frontend && npm run build` 通过；`mvn test` 通过，包含前端构建、后端编译、15 个 MockMvc 集成测试和 1 个向量索引服务测试。当前本机未配置真实 `llama-embedding`，测试覆盖未配置状态、降级行为和模拟命令行重建闭环。

## 2026-05-14 新建笔记表单头部精简

- 按截图反馈移除新建 / 编辑笔记表单卡片顶部的“基础信息 / 新建笔记 / 返回”头部区域，保留工作台顶栏中的取消与保存入口，减少重复层级。
- 清理对应的 `edit-card-heading` 样式和不再使用的返回图标导入。
- 验证：`cd frontend && npm run build` 通过；`mvn process-resources` 通过，并已同步最新前端产物到 `target/classes/static`。

## 2026-05-14 local-cli Embedding 输出解析修复

- 修复 Homebrew 版本 `llama-embedding` 使用 `--log-disable` 时可能不输出 embedding 数组的问题，改为 `--log-verbosity 1`，保留标准输出中的向量数组，同时将后端日志压到错误输出。
- 更新向量索引服务测试，让模拟命令行在 stderr 输出后端日志、stdout 输出向量数组，覆盖真实命令行的输出形态。
- 验证：`mvn test` 通过，包含前端构建、后端编译、15 个 MockMvc 集成测试和 1 个向量索引服务测试。

## 2026-05-14 local-cli 批量向量兜底修复

- 修复批量调用 `llama-embedding` 返回向量数量与输入文本数量不一致时直接导致重建失败的问题；后端现在会自动将该批次降级为逐条生成向量，保证重建索引流程可继续完成。
- 新增批量返回数量不一致的服务测试，覆盖降级为单条 Embedding 后仍能重建 Lucene 向量索引的场景。
- 验证：`mvn test` 通过，包含前端构建、后端编译、15 个 MockMvc 集成测试和 2 个向量索引服务测试。

## 2026-05-14 local-cli 多行文本分隔修复

- 修复逐条 Embedding 兜底时未显式传入 `--embd-separator` 的问题；Homebrew 版本 `llama-embedding` 默认会按换行拆分输入，导致多行笔记即使逐条执行也返回多条向量。
- 后端现在对批量和单条执行都传入自定义分隔符，避免笔记正文、摘要、标签等多行 Embedding 输入被错误拆分。
- 验证：本机用同一条多行文本确认默认分隔符会返回 2 个向量，自定义分隔符只返回 1 个向量；`mvn test` 通过，包含前端构建、后端编译、15 个 MockMvc 集成测试和 2 个向量索引服务测试。

## 2026-05-15 设置页工作台内嵌布局

- 将设置页改为与新建笔记、笔记详情一致的工作台结构，保留左侧固定功能导航不动，右侧展示设置与维护内容。
- 设置页右侧继续保留“功能列表 / 功能区”访问方式，包含工作区概览、索引维护、向量索引、备份恢复和帮助说明。
- 左侧导航补齐分类、标签、回收站和新建分类入口，与编辑页和详情页保持一致。
- 验证：`cd frontend && npm run build` 通过；`mvn process-resources` 通过，并已同步最新前端产物到 `target/classes/static`。

## 2026-05-15 设置页按钮可读性修复

- 为设置页内容区按钮增加作用域样式，普通按钮固定为白底深色文字，主按钮固定为蓝底白色文字，禁用主按钮使用清晰灰色状态。
- 调整设置功能列表按钮为白底深色文字，并强化选中 / hover 边框对比，避免文字与浅蓝背景混在一起。
- 验证：`cd frontend && npm run build` 通过；`mvn process-resources` 通过，并已同步最新前端产物到 `target/classes/static`。

## 2026-05-15 设置页导航入口调整

- 按截图反馈移除设置页左侧固定功能栏底部的“回收站”和“设置”入口，左侧底部仅保留“新建分类”。
- 将“回收站”和当前“设置”入口移动到设置页右上角操作区，以图标按钮形式与“返回工作台”并列展示。
- 为右上角当前设置图标增加选中态样式，保持蓝白主题下的可识别性。
- 验证：`cd frontend && npm run build` 通过；`mvn process-resources` 通过，并已同步最新前端产物到 `target/classes/static`。

## 2026-05-15 全局工作台导航入口统一

- 将列表页、详情页、新建 / 编辑页左侧固定功能栏底部的“回收站”和“设置”入口移除，统一只保留“新建分类”。
- 将“回收站”和“设置”入口统一移动到各页面右上角操作区，列表页保留帮助入口并新增回收站图标，详情页和编辑页新增回收站与设置图标。
- 验证：`cd frontend && npm run build` 通过；`mvn process-resources` 通过，并已同步最新前端产物到 `target/classes/static`。

## 2026-05-15 列表页信息架构第一步重排

- 收敛列表页顶部操作区：保留搜索、新建笔记、更多、帮助、回收站和设置入口，将导入 Markdown、导入书签、导出当前页和下载备份移动到“更多”下拉菜单。
- 精简筛选区：常驻筛选只保留分类和标签，语言、类型、搜索范围、排序、发布状态、更新时间、收藏、置顶和包含已删除移动到“更多筛选”。
- 新增当前视图标题与筛选摘要条，展示当前视图、结果数量和已启用筛选条件，并提供一键清除筛选。
- 移除列表页旧的设置弹窗残留，设置相关维护统一进入独立设置页。
- 验证：`cd frontend && npm run build` 通过；`mvn process-resources` 通过，并已同步最新前端产物到 `target/classes/static`。

## 2026-05-15 详情与编辑页操作层级整理

- 详情页顶部操作区保留返回列表和编辑，将导出、置顶、收藏、发布状态切换、归档、删除、恢复和永久删除统一收进“更多”菜单，减少阅读区按钮干扰。
- 详情页删除和永久删除改用中文确认弹窗，按钮文案保持“确认 / 取消”，危险操作仍需二次确认。
- 新建 / 编辑页表单重组为“基础信息”“AI 辅助”和“更多属性”：标题、分类、发布状态和标签常驻展示，类型、语言、收藏、置顶移动到折叠区。
- 调整表单分组、AI 辅助区和详情页标题卡片样式，继续保持蓝白工作台主题和左侧固定导航布局。
- 验证：`cd frontend && npm run build` 通过；`mvn process-resources` 通过，并已同步最新前端产物到 `target/classes/static`。

## 2026-05-15 设置页按钮对比度增强

- 为设置页根节点增加 `settings-workspace-root` 作用域，统一覆盖设置页顶部、内容区和新建分类弹窗内的 Ant Design 按钮样式。
- 普通按钮固定为白底深色文字和更清晰边框，主按钮固定为深蓝底白色文字，禁用主按钮改为可读的灰蓝底深色文字。
- 补充危险按钮红色高对比状态，避免浅色背景下文字发虚或不可读。
- 验证：`cd frontend && npm run build` 通过；`mvn process-resources` 通过，并已同步最新前端产物到 `target/classes/static`。

## 2026-05-15 新建页返回文案与回收站按钮可读性

- 将新建 / 编辑笔记页顶部“取消”按钮文案调整为“返回列表”，让离开当前表单的意图更明确。
- 为回收站工具条的“恢复当前页”按钮增加专用高对比样式，固定为深蓝底白色文字，hover / focus 状态继续保持白色文字。
- 验证：`cd frontend && npm run build` 通过；`mvn process-resources` 通过，并已同步最新前端产物到 `target/classes/static`。

## 2026-05-15 全局主按钮文字颜色修复

- 修复 Ant Design 主按钮全局样式只设置蓝色背景、未设置文字颜色的问题，避免按钮从 `button { color: inherit; }` 继承黑字导致蓝底黑字。
- 为 `.ant-btn-primary` 统一补充白色文字、白色图标、hover / focus / active 状态白字和禁用态高对比样式。
- 确认当前静态入口引用最新 `index-DPS4-F3z.css`，该文件已包含主按钮白字规则；历史 hash CSS 文件为旧构建残留，不影响当前页面加载。
- 验证：`cd frontend && npm run build` 通过；`mvn process-resources` 通过，并已同步最新前端产物到 `target/classes/static`。

## 2026-05-15 7.3 语义搜索 API

- 新增 `GET /api/search/semantic` 语义搜索接口，使用自然语言问题生成 Embedding，并通过 Lucene `KnnFloatVectorQuery` 从向量索引召回语义相近笔记。
- 向量索引文档补充标签、分类 ID、分类名、语言、发布状态和更新时间字段，语义搜索支持现有筛选条件组合以及分页。
- 新增 `SemanticSearchResultResponse`，返回语义相似度、匹配原因摘要、笔记基本信息和可跳转来源字段。
- 未配置 `local-cli` Embedding 时返回清晰错误；向量索引未创建或为空时返回空分页，不影响普通全文搜索和笔记 CRUD。
- 新增语义搜索 MockMvc 集成测试，覆盖未配置 Embedding、空索引、正常命中、筛选组合和分页边界；`task.md` 标记任务 7.3 完成。
- 验证：`mvn test` 通过，包含前端构建、后端编译、17 个 MockMvc 集成测试和 2 个向量索引服务测试，共 19 个测试。

## 2026-05-15 7.1 统一搜索入口与 7.4 混合搜索排序

- 新增 `SearchMode` 枚举，统一解析 `exact`、`semantic`、`hybrid` 三种搜索模式；`GET /api/search` 增加 `mode` 参数并按模式分发到全文搜索、语义搜索或混合搜索。
- 保留既有 `GET /api/search/semantic` 与 `GET /api/search/hybrid` 兼容入口，避免已有调用路径失效。
- 混合搜索融合 Lucene BM25 标准化得分与向量相似度，并叠加标题命中、标签命中、置顶、收藏、最近更新等轻量加权规则。
- 混合搜索结果返回 `keywordScore`、`semanticSimilarity`、`hybridScore` 和 `rankExplanation`，前端可展示命中来源、相关度和“为什么命中”。
- 前端工作台搜索区新增搜索模式切换：精确全文、语义搜索、混合搜索；URL 查询参数同步 `searchMode`、分页、关键词、范围和筛选条件，刷新后可恢复搜索状态。
- 搜索结果卡片新增命中来源、全文分、语义分、综合分和排序解释展示；语义搜索在未输入自然语言问题时前端给出明确提示，避免直接触发后端错误。
- 新增混合搜索 MockMvc 集成测试，覆盖统一入口三种模式分发、混合排序解释、置顶/收藏加权，以及归档、删除、恢复、重建索引后的脏结果一致性验证。
- `task.md` 标记任务 7.1 和 7.4 完成。
- 验证：`cd frontend && npm run build` 通过；`mvn -Dtest=HybridSearchApiIntegrationTest test` 通过。Vite 仍提示大 chunk，来源为 Vditor / Ant Design Vue 依赖体积，不影响构建结果。

## 2026-05-15 README 智能搜索说明更新

- README 功能概览补充智能搜索能力，说明统一搜索入口支持精确全文、语义搜索和混合搜索。
- 新增“智能搜索”章节，记录 `mode=exact|semantic|hybrid` API 调用方式、兼容入口、混合搜索得分字段和前端排序解释展示。
- 本地 Embedding 配置章节补充向量索引重建接口，当前状态更新为第七阶段 7.1 到 7.4 已完成。
- 验证：本次仅更新 README 与实施日志，未修改功能代码，未执行构建或后端测试。

## 2026-05-15 7.5 RAG 问答、7.6 知识发现与 7.7 搜索运维

- 新增通用 `LlmChatService`，统一封装阿里百炼 / DeepSeek 的 OpenAI 兼容 Chat Completions 调用；`LlmSummaryService` 改为复用该服务，避免重复 HTTP 调用逻辑。
- 新增 `POST /api/knowledge-qa` 知识库问答接口：先复用混合搜索召回相关笔记，再构建引用上下文调用 LLM；回答返回供应商、模型、引用笔记 ID、标题、片段和详情页链接。未配置 LLM 时返回明确 API Key 配置提示。
- 新增 `GET /api/notes/{id}/similar` 相似笔记接口：优先使用向量相似度；向量不可用时回退 Lucene MoreLikeThis；仍不可用时按标签重合度和同分类元数据推荐。
- 新增索引运维接口：`GET /api/admin/index-health` 对比数据库有效笔记、全文索引和向量索引数量；`POST /api/admin/vector-index/cleanup` 清理数据库中已不存在、已删除或已归档笔记对应的向量文档。
- 前端工作台新增“问知识库”面板，支持连续提问、选择 LLM 供应商、设置引用数量、查看引用并跳转原文；当前搜索筛选条件会被用于问答召回。
- 前端搜索区新增搜索历史、常用搜索、清空历史和一键复制当前搜索条件；历史记录存储在浏览器 `localStorage`，不写入后端数据库。
- 详情页新增“可能相关”区域，展示相似笔记标题、推荐原因、来源和相似度，可直接跳转到相关笔记。
- 设置页索引维护补充健康检查结果展示；向量索引面板补充无效向量清理入口；重建全文索引、重建向量索引后会刷新健康状态。
- README 补充知识库问答、相似笔记、索引运维接口、搜索体验和隐私说明；`task.md` 标记 7.5、7.6、7.7 完成。
- 验证：`cd frontend && npm run build` 通过；`mvn test` 通过，包含前端构建、后端编译、20 个 MockMvc 集成测试和 2 个向量索引服务测试，共 22 个测试。Vite 仍提示大 chunk，来源为 Vditor / Ant Design Vue 依赖体积，不影响构建结果。

## 2026-05-15 顶栏标题与首页按钮间距调整

- 移除列表页、详情页、新建 / 编辑页和设置页顶栏左侧的“知识库”标题，避免页面标题区出现重复信息或窄宽度换行。
- 调整顶栏布局，为左侧搜索区和右侧操作区增加稳定间距；首页搜索区改为占用左侧可用宽度，避免“搜索”和“新建笔记”按钮贴在一起。
- 验证：`cd frontend && npm run build` 通过；Vite 仍提示大 chunk，属于既有依赖体积提示，不影响构建结果。

## 2026-05-16 7.8 链接导入预览

- 新增 `POST /api/import/link` 链接导入预览接口，校验仅允许 `http` / `https` 链接，使用 Jsoup 抓取网页并移除脚本、样式、导航、页脚等噪声节点。
- 新增 `LinkImportService`，优先提取 `article` 正文，其次提取 `main`，最后回退到 `body` 文本；抓取超时限制为 15 秒，正文响应体限制为 3MB，提取文本和预览正文均做长度截断。
- 链接导入复用 `LlmSummaryService`，把网页标题和正文交给所选 LLM 供应商生成标题、摘要、标签和分类建议，并返回 Markdown 预览正文；该流程只生成预览，不直接写入笔记表。
- 前端工作台“更多”菜单新增“导入链接”入口，输入 URL 和 LLM 供应商后调用后端预览接口；成功后将临时草稿保存到浏览器 `localStorage`，并跳转到 `/notes/new?draftId=...`。
- 新建笔记页支持读取链接导入临时草稿，自动预填标题、正文、摘要、标签、分类、Markdown 类型和发布状态；读取成功后移除临时草稿，避免重复导入。
- README 补充链接导入入口、API、预览保存流程和隐私提醒；`task.md` 标记任务 7.8 完成。
- 新增 `LinkImportApiIntegrationTest`，使用本地 `HttpServer` 模拟网页与 OpenAI 兼容 Chat Completions 响应，覆盖生成预览、不直接落库和拒绝 `file://` 链接。
- 验证：`mvn test` 通过，包含前端构建、后端编译、22 个既有测试和 2 个链接导入测试，共 24 个测试。Vite 仍提示大 chunk，来源为 Vditor / Ant Design Vue 依赖体积，不影响构建结果。

## 2026-05-16 编辑区视觉美化

- 新建 / 编辑笔记页正文编辑区增加“写作台”标签和 Markdown、图片粘贴、实时预览能力提示，让编辑器入口更像独立写作工作台。
- 为 Vditor 编辑器增加中文 placeholder，并重塑外层卡片、工具栏、编辑画布、焦点态、滚动条和 Markdown 内容样式，降低大面积空白带来的粗糙感。
- 补强 Vditor 工具栏按钮选择器，兼容按钮本身就是 `.vditor-toolbar__item` 的 DOM 结构，确保 hover 和尺寸样式稳定生效。
- 移动端下编辑区标题和能力提示改为纵向排列，避免窄屏按钮和说明拥挤。
- 验证：`cd frontend && npm run build` 通过；浏览器打开 `http://127.0.0.1:5173/notes/new` 检查新建页编辑区，确认新样式、placeholder 和工具栏视觉正常；`mvn -DskipTests process-resources` 通过，并已同步最新前端产物到 `target/classes/static`。Vite 仍提示大 chunk，属于既有依赖体积提示，不影响构建结果。

## 2026-05-16 第八阶段优化方向规划

- 在 `task.md` 新增“第八阶段：体验、稳定性与性能优化”，作为第七阶段智能能力完成后的下一轮迭代方向。
- 第八阶段拆分为 8 个任务：编辑器深度体验、导入链路批量化、搜索与问答质量调优、知识整理工作流、数据安全与可靠性、前端性能与可维护性、配置中心与本地模型引导、测试文档与发布收口。
- 每个任务均以可执行交付项描述，覆盖前端交互、后端能力、隐私提示、测试验证和文档更新，便于后续逐项开发与验收。
- 验证：本次仅更新 `task.md` 和 `docs/implementation-log.md`，未修改运行代码，未执行构建或后端测试。

## 2026-05-16 8.1 编辑器深度体验优化

- 新建 / 编辑笔记页新增“专注写作”模式，切换后隐藏左侧导航和基础信息表单，主区域只保留正文编辑区、顶部返回和保存入口。
- 编辑器顶部能力提示补充“拖拽上传”，正文编辑器底部新增状态条，展示字符数、行数、图片数、保存状态和最近保存时间。
- 增加未保存状态跟踪：正文、标题、标签、分类、发布状态等表单字段变更后显示“有未保存修改”，保存中和已保存状态使用不同视觉提示。
- 图片上传体验统一为状态条：粘贴图片和拖拽图片均复用本地图片上传 API，显示当前文件名、文件大小、上传进度、成功结果和失败重试入口。
- 链接导入草稿进入新建页时保持“未保存修改”状态，避免临时草稿被误判为已保存。
- 移动端下编辑器标题、状态条、上传提示和操作区纵向排列，降低窄屏拥挤风险。
- `task.md` 标记任务 8.1 完成。
- 验证：`cd frontend && npm run build` 通过；浏览器打开 `http://127.0.0.1:5173/notes/new` 验证专注写作模式、正文输入后的统计与未保存状态；`mvn -DskipTests process-resources` 通过，并已同步最新前端产物到 `target/classes/static`。Vite 仍提示大 chunk，属于既有依赖体积提示，不影响构建结果。
